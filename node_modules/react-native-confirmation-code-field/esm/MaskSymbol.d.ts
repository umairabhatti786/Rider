import type { ReactNode } from 'react';
import React from 'react';
export declare const DEFAULT_BLINKING_SPEED = 500;
interface MaskSymbolProps {
    delay?: number;
    children: ReactNode;
    maskSymbol: ReactNode;
    isLastFilledCell: boolean;
}
export declare function MaskSymbol({ isLastFilledCell, children: symbol, maskSymbol, delay, }: MaskSymbolProps): React.JSX.Element;
export declare namespace MaskSymbol {
    var displayName: string;
}
export {};
