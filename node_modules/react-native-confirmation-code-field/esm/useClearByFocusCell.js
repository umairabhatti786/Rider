import { useMemo } from 'react';
import { useClearByFocusCellCommon } from './useClearByFocusCell.common';
export const useClearByFocusCell = (params) => {
    'use memo';
    const [clearCodeByCoords, getCellOnLayoutHandler] = useClearByFocusCellCommon(params);
    const inputProps = useMemo(() => ({
        onPressOut: function onInputPressOut(event) {
            clearCodeByCoords(event.nativeEvent);
        },
    }), [clearCodeByCoords]);
    return [inputProps, getCellOnLayoutHandler];
};
