import { useRef, useEffect } from 'react';
export function useBlurOnFulfill({ value, cellCount, }) {
    'use memo';
    const inputRef = useRef(null);
    useEffect(function blurOnFulfillEffect() {
        if (value?.length === cellCount) {
            inputRef.current?.blur();
        }
    }, [value, cellCount]);
    return inputRef;
}
